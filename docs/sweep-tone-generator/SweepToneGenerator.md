Tone Generator PRO – генератор тона развертки (линейный или логарифмический для музыкантов).

## MVP в NeuroSync Pro (Windows, realtime)

На текущем этапе в NeuroSync Pro реализован минимальный realtime‑скелет под Windows:

- `neurosync-pro tone` — непрерывный тон
- `neurosync-pro sweep` — sweep (linear/log), loop, fade in/out
- `neurosync-pro sweep-ui` — простое окно управления tone/sweep

Установка:

```bash
pip install -e ".[audio]"
pip install -e ".[gui]"
```

Примеры:

```bash
neurosync-pro tone --freq 440 --duration 2
neurosync-pro sweep --f0 200 --f1 1000 --duration 10
neurosync-pro sweep --f0 100 --f1 8000 --duration 30 --log --loop
neurosync-pro sweep-ui
```

## Ориентир по функциям (из Tone Generator PRO)

Возможности:

Тон-генератор
Генератор развертки
Многотональный генератор с 16 каналами (в режиме реального времени)
Генератор белого шума
Генератор розового шума
Генератор функций
Генератор программ
Генератор аудиофайлов с многоканальным режимом до 5.1
Ультразвуковой генератор (на ЦАП 48 кГц или 96 кГц)
Удобный интерфейс
Работает в фоновом режиме
Множество готовых к использованию примеров

Расширенные возможности:

Надежный, с двойной точностью, в режиме реального времени, точный, генератор волн (64-битная точность IEEE 754 с плавающей запятой)
Ручное увеличение или уменьшение частоты в реальном времени (точно, без хлопков и щелчков)
Циклы (непрерывно, без задержек, без кликов)
Амплитудная модуляция
16-канальный многотональный генератор в реальном времени
Сохраняйте пользовательскую конфигурацию в качестве предустановки для повторного использования в будущем.
Точная регулировка громкости левого/правого канала
Точная настройка времени появления и затухания
Возможность инвертировать один канал для подавления эффектов.
Много, много предварительно запрограммированных звуковых тестов, готовых к использованию профессиональным звукорежиссером.

Что вы можете делать с этим приложением:

Тесты динамиков
Точная настройка аудиосистемы
Создавайте звуки релаксации и медитации.
Шумовая маска
Розовый шум вызывает сонливость.
Найдите свою частоту шума в ушах.
Тест на возрастную потерю слуха.
Очиститель динамиков: удалите воду и пыль с динамиков телефона.
Узнайте много фактов о звуке
Ваше воображение — это предел!!



https://play.google.com/store/apps/details?id=com.jm.android.frequencygenerator